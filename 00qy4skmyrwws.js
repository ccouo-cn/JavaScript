const BASE = "https://api.yuzusoft.pw/tg.go"; /* 通用：显示/隐藏代码块*/ function toggleCode(id) { const el = document.getElementById(id); el.classList.toggle("show"); if (el.classList.contains("show")) { hljs.highlightElement(el.querySelector("code")); } } /* 通用：复制代码*/ function copyCode(id) { const code = document.getElementById(id).querySelector("code").innerText; navigator.clipboard.writeText(code).then(() => { const btn = document.querySelector("#" + id + " .code-header button"); const old = btn.innerText; btn.innerText = "已复制"; setTimeout(() => { btn.innerText = old; }, 1500); }); } /* ========= 发消息 ========= */ function sendMessage() { const chat_id = document.getElementById("chatId").value.trim(); const text = document.getElementById("text").value.trim(); fetch(`${BASE}?action=sendMessage`, { method:"POST", headers:{"Content-Type":"application/x-www-form-urlencoded"}, body: new URLSearchParams({chat_id, text}) }) .then(r=>r.json()) .then(d=>{ document.getElementById("msgResult").innerText = JSON.stringify(d, null, 2); }); } document.getElementById("msgCodeInner").innerText = `fetch('https://api.yuzusoft.pw/tg.go?action=sendMessage', { method: 'POST', headers: { 'Content-Type': 'application/x-www-form-urlencoded' }, body: new URLSearchParams({ chat_id: '你的ChatID', text: '你要发送的文字' }) }) .then(res => res.json()) .then(data => console.log(JSON.stringify(data, null, 2)));`; /* ========= 发位置 ========= */ function sendLocation() { const chat_id = document.getElementById("chatId").value.trim(); const latitude = document.getElementById("lat").value; const longitude = document.getElementById("lng").value; fetch(`${BASE}?action=sendLocation`, { method:"POST", headers:{"Content-Type":"application/x-www-form-urlencoded"}, body: new URLSearchParams({chat_id, latitude, longitude}) }) .then(r=>r.json()) .then(d=>{ document.getElementById("locResult").innerText = JSON.stringify(d, null, 2); }); } document.getElementById("locCodeInner").innerText = `fetch('https://api.yuzusoft.pw/tg.go?action=sendLocation', { method: 'POST', headers: { 'Content-Type': 'application/x-www-form-urlencoded' }, body: new URLSearchParams({ chat_id: '你的ChatID', latitude: '39.9087', longitude: '116.3975' }) }) .then(res => res.json()) .then(data => console.log(JSON.stringify(data, null, 2)));`; /* ========= 发文件 ========= */ function sendFile() { const chat_id = document.getElementById("chatId").value.trim(); const file = document.getElementById("fileInput").files[0]; if(!file){alert("请先选择文件");return;} const fd = new FormData(); fd.append("chat_id", chat_id); fd.append("document", file); fd.append("caption", "来自中转API的文件"); fetch(`${BASE}?action=sendDocument`, { method:"POST", body: fd }) .then(r=>r.json()) .then(d=>{ document.getElementById("fileResult").innerText = JSON.stringify(d, null, 2); }); } document.getElementById("fileCodeInner").innerText = `const formData = new FormData(); formData.append('chat_id', '你的ChatID'); formData.append('document', document.querySelector('input[type=file]').files[0]); formData.append('caption', '文件说明'); fetch('https://api.yuzusoft.pw/tg.go?action=sendDocument', { method: 'POST', body: formData }) .then(res => res.json()) .then(data => console.log(JSON.stringify(data, null, 2)));`; /* ========= 发图片 ========= */ function sendPhoto() { const chat_id = document.getElementById("chatId").value.trim(); const file = document.getElementById("photoInput").files[0]; if(!file){alert("请先选择图片");return;} const fd = new FormData(); fd.append("chat_id", chat_id); fd.append("photo", file); fd.append("caption", "来自中转API的图片"); fetch(`${BASE}?action=sendPhoto`, { method:"POST", body: fd }) .then(r=>r.json()) .then(d=>{ document.getElementById("photoResult").innerText = JSON.stringify(d, null, 2); }); } document.getElementById("photoCodeInner").innerText = `const formData = new FormData(); formData.append('chat_id', '你的ChatID'); formData.append('photo', document.querySelector('input[type=file]').files[0]); formData.append('caption', '图片说明'); fetch('https://api.yuzusoft.pw/tg.go?action=sendPhoto', { method: 'POST', body: formData }) .then(res => res.json()) .then(data => console.log(JSON.stringify(data, null, 2)));`; /* ========= 发选择按钮 ========= */ function sendKeyboard() { const chat_id = document.getElementById("chatId").value.trim(); const reply_markup = { inline_keyboard: [ [ {text:"选项A", callback_data:"a"}, {text:"选项B", callback_data:"b"} ] ] }; fetch(`${BASE}?action=sendMessage`, { method:"POST", headers:{"Content-Type":"application/x-www-form-urlencoded"}, body: new URLSearchParams({ chat_id, text:"请选择一个选项", reply_markup: JSON.stringify(reply_markup) }) }) .then(r=>r.json()) .then(d=>{ document.getElementById("kbResult").innerText = JSON.stringify(d, null, 2); }); } document.getElementById("kbCodeInner").innerText = `const reply_markup = { inline_keyboard: [ [ {text: "选项A", callback_data: "a"}, {text: "选项B", callback_data: "b"} ] ] }; fetch('https://api.yuzusoft.pw/tg.go?action=sendMessage', { method: 'POST', headers: { 'Content-Type': 'application/x-www-form-urlencoded' }, body: new URLSearchParams({ chat_id: '你的ChatID', text: '请选择一个选项', reply_markup: JSON.stringify(reply_markup) }) }) .then(res => res.json()) .then(data => console.log(JSON.stringify(data, null, 2)));`; /* ========= getMe ========= */ function getMe() { fetch(`${BASE}?action=getMe`) .then(r=>r.json()) .then(d=>{ document.getElementById("meResult").innerText = JSON.stringify(d, null, 2); }); } document.getElementById("meCodeInner").innerText = `fetch('https://api.yuzusoft.pw/tg.go?action=getMe') .then(res => res.json()) .then(data => console.log(JSON.stringify(data, null, 2)));`; /* ========= getUpdates ========= */ function getUpdates() { fetch(`${BASE}?action=getUpdates`) .then(r=>r.json()) .then(d=>{ document.getElementById("upResult").innerText = JSON.stringify(d, null, 2); }); } document.getElementById("upCodeInner").innerText = `fetch('https://api.yuzusoft.pw/tg.go?action=getUpdates') .then(res => res.json()) .then(data => { /* 从返回的 result 数组中找到第一条消息*/ const chatId = data.result[0].message.chat.id; const username = data.result[0].message.from.username; console.log('Chat ID:', chatId); console.log('Username:', username); console.log(JSON.stringify(data, null, 2)); });`;